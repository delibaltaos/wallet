import Wallet from './src/wallet.js';

// Wallet.getMyTokens().then(tokens => {
//     console.log(tokens);
// }).catch(error => {
//     console.log(error);
// });

// Wallet.listenNewTokens(async baseMint => {
//     console.log('new token : ', baseMint);
//     // await Wallet.buy(baseMint, 1);
// });